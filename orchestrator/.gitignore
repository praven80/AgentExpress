
# --- the UI's build toolchain ---------------------------------------------
# web/ is now a Vite + React + Cloudscape app. `dist/` is generated by
# `npm run build` and is what both IaC paths upload, so it is a build artifact
# rather than source.
web/node_modules/
web/dist/
