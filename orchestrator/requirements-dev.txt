# Local development only (running the dev server, the tests, optional real MCP).
# Installs the runtime deps plus the local web server and MCP adapters.
-r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
langchain-mcp-adapters>=0.1.0
# The config-plane test suite (see tests/README.md). Run it from orchestrator/
# with `pytest` — no AWS credentials and no model calls, about a second.
pytest>=8.0
