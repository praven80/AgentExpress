// Generated by Terraform. Non-secret Cognito settings for the SPA.
// When enabled is false the UI runs with no authentication.
window.AUTH_CONFIG = {
  enabled: ${enabled},
  region: "${region}",
  userPoolId: "${user_pool_id}",
  clientId: "${client_id}",
  domainPrefix: "${domain_prefix}"
};
